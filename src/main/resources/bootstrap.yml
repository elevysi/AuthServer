server:
  port: 8100
#  context-path: /auth
  session:
    cookie:
      name: ELAUTHSESSION
spring:
  application:
    name: authservice
  profiles:
    active:
      default
  cloud:
    config:
      uri: http://localhost:8888
      enabled: true